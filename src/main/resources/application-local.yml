spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/artvu
    username: artvu
    password: 1234

  jpa:
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    defer-datasource-initialization: true

#  sql:
#    init:
#      mode: always

  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: true

  open-api:
    secretKey: "9bc5a206710943769d657e163212035d"
    kakaoAdminKey : "5d94fcffa2a7be264a6f09015b4709a8"


  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: '8611cd3341ff998c7fddc8764cf1e43a'
            client-secret: 'HStQmeJF6qWaeX4SaWQNeY8tCQOB68qO'
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope: account_email, profile_image, profile_nickname, gender
            client-name: kakao
        provider:
          kakao:
            authorizationUri: https://kauth.kakao.com/oauth/authorize
            tokenUri: https://kauth.kakao.com/oauth/token
            userInfoUri: https://kapi.kakao.com/v2/user/me
            userNameAttribute: id

app:
  auth:
    tokenSecret: '1438548D548538CA131349ADFD18238BDB'
    tokenExpiry: 1800000 # 30분
    refreshTokenExpiry: 604800000 # 7일
  oauth2:
    authorized-redirect-uris: http://localhost:3000/oauth/redirect
cors:
  allowed-headers: '*'
  max-age: 3600
  allowed-methods: GET,POST,PUT,PATCH,DELETE,OPTIONS
  allowed-origins: '*'
springdoc:
  show-login-endpoint: true

logging:
  file:
    path: /back-end/logfiles
  logback:
    rolling policy:
      file-name-pattern: /log-backup/service.%d{yyyy-MM-dd}-%i.log
      max-file-size: 10KB
  pattern:
    dateformat: Asia/Seoul